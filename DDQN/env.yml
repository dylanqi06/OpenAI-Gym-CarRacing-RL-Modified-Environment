name: hpc_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - scipy
  - gym
  - opencv
  - pyvirtualdisplay
  - tensorflow
  - tqdm
  - matplotlib  # For plotResults dependencies
  - pip
  - pip:
      - plot-results  # Replace with actual package or script if needed
